import 'package:flutter/material.dart';

const kPaddingSymmetricHorizontal20 = EdgeInsets.symmetric(horizontal: 20);

const kContainerBorderRadius = BorderRadius.only(
  topRight: Radius.circular(10),
  topLeft: Radius.circular(10),
);
